# React Cinema Booking App

Small booking cinema app.

## Запуск проекта

Для работы с проектом вам понадобится [Node.js](https://nodejs.org/).

### 1. Установка зависимостей

Перед первым запуском установите все необходимые пакеты:

```bash
npm install
```

### 2. Запуск проекта (Рекомендуется для разработки)

Чтобы запустить приложение с использованием Mock Service Worker (MSW) для имитации бэкенда:

```bash
npm start
```

- Приложение будет доступно по адресу: [http://localhost:3000](http://localhost:3000)
- API запросы будут перехватываться MSW.

### 3. Обычный запуск (без моков)

Для запуска приложения без моков (ожидается реальный бэкенд), можно запустить webpack-dev-server напрямую или настроить отдельный скрипт без переменной REACT_APP_USE_MOCK.

### 4. Тестирование

Для запуска тестов:

```bash
npm test
```

### 5. Линтинг

Для проверки кода линтером:

```bash
npm run lint
```
